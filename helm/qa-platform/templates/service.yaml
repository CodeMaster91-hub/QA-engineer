apiVersion: v1
kind: Service
metadata:
  name: {{ include "qa-platform.fullname" . }}
  labels:
    {{- include "qa-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qa-platform.selectorLabels" . | nindent 4 }}
